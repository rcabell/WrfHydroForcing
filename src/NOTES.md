* create FEException subclasses for different components (config, etc)
* config defaults (ie, "None" for bias-correction)
* create config section superclass with validation support
* move regrid to input and interpolation to output
* config default to none or 0, or enfore required
* moce configuration section classes to modules that define section logic?